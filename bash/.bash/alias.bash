#
# Bash aliases
# 

alias cp="cp -i"                          # confirm before overwriting something
alias df='df -h'                          # human-readable sizes
alias free='free -h'                      # human-readable sizes
alias more=less
alias ll='ls -l'
alias la='ls -a'
alias lal='ls -a -l'

alias p='sudo pacman'
alias bw="wal -i ~/.config/wall.png"
alias school='cd ~/Documents/School'

alias loopback='pactl load-module module-loopback'

# Config Shortcuts 
alias brc='vim ~/.bashrc'
alias config='vim ~/.config/i3/config'
alias nf='vim ~/.config/neofetch/config.conf'
alias pb='vim ~/.config/polybar/config'
alias compc='vim ~/.config/compton.conf'
alias rc='vim ~/.config/ranger/rc.conf'
alias bsu='source ~/.bashrc'

