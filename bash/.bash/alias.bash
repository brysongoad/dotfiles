#
# Bash aliases
# 

alias cp="cp -i"                          # confirm before overwriting something
alias df='df -h'                          # human-readable sizes
alias free='free -h'                      # human-readable sizes
alias more=less
alias ll='ls -l'
alias la='ls -a'
alias lal='ls -a -l'
alias rm='rm -i'
alias vim='nvim'

alias p='sudo pacman'
# alias bw="wal -i ~/.config/wall.png"

# listen to line in audio input
alias loopback='pactl load-module module-loopback'
